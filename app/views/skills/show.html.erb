<div class="banner" style="background-image: url(<%= @skill.img_url ? @skill.img_url : cl_image_path(@skill.photo.key) %>)">
</div>
<div class="container my-5">
  <h2 class="my-4"> <%= @skill.name %> </h2>
  <div class="row">
    <div class="col-lg-9">
      <div class="skill-card">
        <div class="skill-info1">
          <p><%= @skill.description %></p>
        </div>
        <div class="skill-info2">
          <p>Skill level: <strong><%= @skill.level %></strong></p>
          <p><strong><%= @skill.price %> €/day</strong></p>
        </div>
      </div>
    </div>
    <div class="col-lg-3">
      <%= render "bookings/form" %>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-10 text-start">
        <%= link_to "Back to list", skills_path, class: "btn-ghost" %>
    </div>
  </div>
</div>
